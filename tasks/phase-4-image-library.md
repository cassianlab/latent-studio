# 阶段 4：图片生成队列与项目内容库

依赖：`tasks/phase-3-model-text.md`

## 已完成

- [x] OpenAI-compatible 图片生成与图片编辑适配器，支持 data/base64 和项目内文件参考图。
- [x] 图片连接级并发队列，支持用户配置的 1-64 并发上限、暂停、恢复、取消、重试和事件流。
- [x] 图片模型按模型配置 ID 路由，文本/图片供应商连接和 API Key 保持独立。
- [x] 项目内参考图 canonical realpath 校验，阻止路径穿越、符号链接越界和超过 20 MiB 的文件。
- [x] 全局提示词/记忆存储，兼容已有 SQLite 表，支持同名版本、quick save 和记忆状态编辑。
- [x] 项目提示词/记忆原子 JSON 存储，写入 `.latent-studio/`，支持同名版本和重开恢复。
- [x] 项目文件/目录只读元数据服务，支持隐藏文件开关、递归和条目上限。
- [x] 图片任务 IPC、内容库 IPC 和 Preload 窄边界接入；当前项目根目录由主进程注入。

## 已完成补充

- [x] 图片结果下载 URL 的 SSRF 校验、格式校验和项目 `outputs/` 原子落盘（尺寸对齐仍待接入）。
- [x] 任务中心持久化、重启恢复和跨窗口订阅。
- [x] 图片工作台真实提交、任务状态订阅和并发队列预览；智能变体分发已接入 Agent 工具循环。
- [x] 素材导入、参考图快速选择和项目素材元数据 UI。
- [x] 提示词库/记忆页面改为真实 IPC 数据和保存操作。

验证记录（2026-09-13）：`npm test`（26 个测试文件、105 个测试）、`npm run build:app`、`npm run check:file-size`、`git diff --check` 通过。图片结果会优先使用主进程项目输出路径；批量队列处理了事件先到达 UI 的竞态；标注编辑器已能将原图与标注合成图提交为编辑任务。
